# Secret key for Google Gemini (AI service that reads and answers questions)
GEMINI_API_KEY="your_gemini_api_key_here"

# Secret key for Hugging Face (AI service that creates smart word numbers called embeddings)
HF_API_KEY="your_huggingface_api_key_here"

# Connection address for the PostgreSQL database (the digital filing cabinet for saving data)
DATABASE_URL="postgresql://postgres:postgres@db:5432/doc_qa"
